class lexicon(object):

    def __init__(self, stuff):
        pass
        # stuff = raw_input('>')
        #words = lex.spilt()

    def scan(self):
        
        stuff = raw_input('>')
        words = lex.spilt()
        for word in words:
            if word = 'go' or 'kill' or 'eat'
                result + = ['verb', %s] %word
            elif word = 'the' or 'in' or 'of'
                result + = ['stop', %s] %word
                
